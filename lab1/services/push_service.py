class PushService:
    def send(self, device_id, message):
        # Here you would integrate with an actual push notification service
        print(f"Sending Push Notification to {device_id}: {message}")
